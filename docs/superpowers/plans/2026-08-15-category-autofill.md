# Category Autofill 插件实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 实现 Obsidian 插件「Category Autofill」：自动把笔记 frontmatter 的 `category` 填充为该文件直接父文件夹名，支持事件实时更新与全库批量命令。

**Architecture:** 纯逻辑（路径判定/写入决策）放在 `logic.ts`，与 Obsidian API 完全解耦，用 Node 内置 `node:test` 做单元测试；`main.ts` 只做 API 接线（事件监听、`processFrontMatter` 写入、命令、设置页），构建用 esbuild（官方示例模板结构），零运行时依赖。

**Tech Stack:** TypeScript 5、esbuild、Obsidian API（`fileManager.processFrontMatter`）、node:test、pnpm。

## Global Constraints

- 项目根目录 = `E:\GithubProgect\MyRunProject\dumplingandcakeblog\obsidian-category-autofill`（已被博客仓库 gitignore，本目录是独立 git 仓库，分支默认 master）
- 零运行时第三方依赖；devDependencies 仅 obsidian / typescript / esbuild / builtin-modules / @types/node / tslib
- 属性键固定为 `category`；多层目录只取直接父文件夹名
- 跳过规则：库根目录文件、隐藏目录文件（路径任一段以 `.` 开头）、非 md 文件
- 设置项「覆盖已有值」默认开启；category 已等于目标值时不写入（不得产生无意义文件改动）
- 界面文案中文；构建产物 `main.js`（cjs、external obsidian）
- Node ≥ 22；包管理器 pnpm；逻辑文件只允许可擦除 TS 语法（无 enum/namespace，Node 直接跑 .ts）
- 提交格式 `<type>(<scope>): <描述>`；每个任务结束时提交一次

---

### Task 1: 项目脚手架与构建链

**Files:**
- Create: `package.json`
- Create: `tsconfig.json`
- Create: `manifest.json`
- Create: `versions.json`
- Create: `esbuild.config.mjs`
- Create: `main.ts`（最小占位实现，Task 3 替换为完整实现）

**Interfaces:**
- Produces: 可运行的构建链；命令 `pnpm install` / `pnpm build` / `pnpm typecheck` / `pnpm test` / `pnpm dev`

- [ ] **Step 1: 确认环境版本**

Run:
```bash
node --version
pnpm --version
```
Expected: node ≥ 22.18（22.18 起原生支持运行 .ts，测试脚本依赖此特性）；pnpm 任意 9.x。若 node < 22.18，把 `package.json` 的 test 脚本改为 `node --experimental-strip-types --test logic.test.ts`，其余不变。

- [ ] **Step 2: 写 package.json**

```json
{
	"name": "obsidian-category-autofill",
	"version": "1.0.0",
	"description": "自动把笔记的 category 属性填充为所在文件夹的名称",
	"type": "module",
	"scripts": {
		"dev": "node esbuild.config.mjs",
		"build": "pnpm typecheck && node esbuild.config.mjs production",
		"typecheck": "tsc --noEmit",
		"test": "node --test logic.test.ts"
	},
	"devDependencies": {
		"@types/node": "^22.10.0",
		"builtin-modules": "^5.0.0",
		"esbuild": "^0.24.2",
		"obsidian": "^1.8.7",
		"tslib": "^2.8.1",
		"typescript": "^5.7.3"
	}
}
```

- [ ] **Step 3: 写 tsconfig.json**

```json
{
	"compilerOptions": {
		"target": "ES2022",
		"module": "ESNext",
		"moduleResolution": "Bundler",
		"lib": ["DOM", "ES2022"],
		"strict": true,
		"noEmit": true,
		"allowImportingTsExtensions": true,
		"isolatedModules": true,
		"esModuleInterop": true,
		"forceConsistentCasingInFileNames": true,
		"noUnusedLocals": true,
		"noUnusedParameters": true,
		"skipLibCheck": true,
		"types": ["node"]
	},
	"include": ["main.ts", "logic.ts", "logic.test.ts"]
}
```

- [ ] **Step 4: 写 manifest.json**

```json
{
	"id": "category-autofill",
	"name": "Category Autofill",
	"version": "1.0.0",
	"minAppVersion": "0.15.0",
	"description": "自动把笔记的 category 属性填充为所在文件夹的名称",
	"author": "dumplingandcake",
	"isDesktopOnly": false
}
```

- [ ] **Step 5: 写 versions.json**

```json
{
	"1.0.0": "0.15.0"
}
```

- [ ] **Step 6: 写 esbuild.config.mjs**

```js
import process from "process";
import builtins from "builtin-modules";
import esbuild from "esbuild";

const banner = `/*
THIS IS A GENERATED/BUNDLED FILE BY ESBUILD
*/
`;

const prod = process.argv[2] === "production";

const context = await esbuild.context({
	banner: { js: banner },
	entryPoints: ["main.ts"],
	bundle: true,
	external: [
		"obsidian",
		"electron",
		"@codemirror/autocomplete",
		"@codemirror/collab",
		"@codemirror/commands",
		"@codemirror/language",
		"@codemirror/lint",
		"@codemirror/search",
		"@codemirror/state",
		"@codemirror/view",
		"@lezer/common",
		"@lezer/highlight",
		"@lezer/lr",
		...builtins,
	],
	format: "cjs",
	target: "es2018",
	logLevel: "info",
	sourcemap: prod ? false : "inline",
	treeShaking: true,
	outfile: "main.js",
});

if (prod) {
	await context.rebuild();
	process.exit(0);
} else {
	await context.watch();
}
```

- [ ] **Step 7: 写占位 main.ts（保证构建链先跑通）**

```ts
import { Plugin } from "obsidian";

export default class CategoryAutofillPlugin extends Plugin {
	async onload() {
		console.log("Category Autofill loaded");
	}
}
```

- [ ] **Step 8: 安装依赖**

Run: `pnpm install`
Expected: 成功安装 6 个 devDependencies，生成 `pnpm-lock.yaml`、`node_modules/`（已被 .gitignore 忽略）。

- [ ] **Step 9: 验证构建与类型检查**

Run: `pnpm build` → Expected: typecheck 无输出通过，esbuild 输出 `main.js  8.0kb` 左右并退出 0。
Run: `pnpm typecheck` → Expected: 无输出，退出 0。

- [ ] **Step 10: 提交**

```bash
git add package.json pnpm-lock.yaml tsconfig.json manifest.json versions.json esbuild.config.mjs main.ts
git commit -m "chore(scaffold): 初始化插件脚手架与 esbuild 构建链"
```

---

### Task 2: 纯逻辑模块与单元测试（TDD）

**Files:**
- Create: `logic.ts`
- Create: `logic.test.ts`

**Interfaces:**
- Produces（后续任务依赖，签名不得改动）:
  - `isHiddenPath(path: string): boolean` — 路径任一段以 `.` 开头
  - `isEmptyCategory(value: unknown): boolean` — undefined/null/空串/纯空白串/空数组视为空
  - `getTargetCategory(path: string, parentName: string, parentIsRoot: boolean): string | null` — 根目录或隐藏目录返回 null（跳过），否则返回 parentName
  - `shouldUpdateCategory(current: unknown, target: string, overwrite: boolean): boolean` — current===target 恒 false；否则覆盖开启恒 true，关闭时仅 current 为空返回 true

- [ ] **Step 1: 写失败测试 logic.test.ts**

```ts
import assert from "node:assert/strict";
import { describe, it } from "node:test";
import {
	getTargetCategory,
	isEmptyCategory,
	isHiddenPath,
	shouldUpdateCategory,
} from "./logic.ts";

describe("isHiddenPath", () => {
	it("普通路径返回 false", () => {
		assert.equal(isHiddenPath("技术分享/前端/x.md"), false);
		assert.equal(isHiddenPath("x.md"), false);
	});

	it("隐藏目录返回 true", () => {
		assert.equal(isHiddenPath(".obsidian/plugins/x.md"), true);
		assert.equal(isHiddenPath("技术分享/.trash/x.md"), true);
	});

	it("隐藏文件本身也算隐藏路径", () => {
		assert.equal(isHiddenPath(".hidden.md"), true);
	});
});

describe("isEmptyCategory", () => {
	it("缺失、null、空串、纯空白、空数组视为空", () => {
		assert.equal(isEmptyCategory(undefined), true);
		assert.equal(isEmptyCategory(null), true);
		assert.equal(isEmptyCategory(""), true);
		assert.equal(isEmptyCategory("   "), true);
		assert.equal(isEmptyCategory([]), true);
	});

	it("非空值不算空", () => {
		assert.equal(isEmptyCategory("前端"), false);
		assert.equal(isEmptyCategory(["前端"]), false);
		assert.equal(isEmptyCategory(0), false);
	});
});

describe("getTargetCategory", () => {
	it("根目录文件跳过（即使父名为库名）", () => {
		assert.equal(getTargetCategory("x.md", "我的库", true), null);
	});

	it("隐藏目录中的文件跳过", () => {
		assert.equal(getTargetCategory(".obsidian/x.md", "obsidian", false), null);
	});

	it("子文件夹文件返回直接父文件夹名", () => {
		assert.equal(getTargetCategory("技术分享/前端/x.md", "前端", false), "前端");
	});
});

describe("shouldUpdateCategory", () => {
	const target = "前端";

	it("值已等于目标值时不写", () => {
		assert.equal(shouldUpdateCategory("前端", target, true), false);
	});

	it("覆盖开启时总是写", () => {
		assert.equal(shouldUpdateCategory("后端", target, true), true);
		assert.equal(shouldUpdateCategory("", target, true), true);
	});

	it("覆盖关闭时仅在为空时写", () => {
		assert.equal(shouldUpdateCategory("", target, false), true);
		assert.equal(shouldUpdateCategory("后端", target, false), false);
		assert.equal(shouldUpdateCategory(undefined, target, false), true);
	});
});
```

- [ ] **Step 2: 运行测试确认失败**

Run: `pnpm test`
Expected: FAIL——`Cannot find module './logic.ts'`（logic.ts 尚不存在）。

- [ ] **Step 3: 写最小实现 logic.ts**

```ts
/** 纯逻辑模块：不依赖 Obsidian API，可直接被 node:test 测试。 */

/** 路径任一段以 "." 开头（如 .obsidian、.trash）则视为隐藏目录。 */
export function isHiddenPath(path: string): boolean {
	return path.split("/").some((segment) => segment.startsWith("."));
}

/** 判断现有 category 值是否为空（缺失、null、空串、纯空白、空数组都算空）。 */
export function isEmptyCategory(value: unknown): boolean {
	if (value === undefined || value === null) return true;
	if (typeof value === "string") return value.trim() === "";
	if (Array.isArray(value)) return value.length === 0;
	return false;
}

/**
 * 计算该文件应填入的 category 值。
 * 返回 null 表示跳过：库根目录文件或隐藏目录中的文件。
 */
export function getTargetCategory(
	path: string,
	parentName: string,
	parentIsRoot: boolean,
): string | null {
	if (parentIsRoot) return null;
	if (isHiddenPath(path)) return null;
	return parentName;
}

/**
 * 判断是否需要写入。
 * current === target 时不写（避免无意义文件改动）；
 * 覆盖开关关闭时仅在当前值为空的情况下写。
 */
export function shouldUpdateCategory(
	current: unknown,
	target: string,
	overwrite: boolean,
): boolean {
	if (current === target) return false;
	if (overwrite) return true;
	return isEmptyCategory(current);
}
```

- [ ] **Step 4: 运行测试确认通过**

Run: `pnpm test`
Expected: PASS，15 个用例全绿（`# pass 15`），退出 0。

- [ ] **Step 5: 提交**

```bash
git add logic.ts logic.test.ts
git commit -m "feat(logic): 纯逻辑模块与单元测试（路径判定/写入决策）"
```

---

### Task 3: 插件主体——设置、事件监听与写入

**Files:**
- Modify: `main.ts`（占位实现替换为完整实现）

**Interfaces:**
- Consumes: `getTargetCategory(path, parentName, parentIsRoot)`、`shouldUpdateCategory(current, target, overwrite)`（Task 2）
- Produces: `CategoryAutofillPlugin`（默认导出）、`updateCategory(file: TFile): Promise<UpdateResult>`、`runBatch(): Promise<void>`（Task 4 挂命令用）

- [ ] **Step 1: 用完整实现替换 main.ts**

```ts
import { Notice, Plugin, PluginSettingTab, Setting, TAbstractFile, TFile, TFolder } from "obsidian";
import type { App } from "obsidian";
import { getTargetCategory, shouldUpdateCategory } from "./logic.ts";

interface CategoryAutofillSettings {
	overwriteExisting: boolean;
}

const DEFAULT_SETTINGS: CategoryAutofillSettings = {
	overwriteExisting: true,
};

/** 防抖时间（毫秒）：同一路径在此窗口内的重复事件只处理一次。 */
const DEBOUNCE_MS = 200;

type UpdateResult = "updated" | "skipped" | "failed";

export default class CategoryAutofillPlugin extends Plugin {
	settings: CategoryAutofillSettings = DEFAULT_SETTINGS;

	/** 防抖队列：路径 -> 待执行定时器。 */
	private pending = new Map<string, number>();

	async onload() {
		await this.loadSettings();

		this.registerEvent(
			this.app.vault.on("create", (file) => this.handleCreate(file)),
		);
		this.registerEvent(
			this.app.vault.on("rename", (file, oldPath) =>
				this.handleRename(file, oldPath),
			),
		);

		this.addSettingTab(new CategoryAutofillSettingTab(this.app, this));
	}

	onunload() {
		for (const timer of this.pending.values()) {
			window.clearTimeout(timer);
		}
		this.pending.clear();
	}

	async loadSettings() {
		this.settings = Object.assign({}, DEFAULT_SETTINGS, await this.loadData());
	}

	async saveSettings() {
		await this.saveData(this.settings);
	}

	/** 计算该文件的 category 目标值；需要跳过时返回 null。 */
	private targetFor(file: TFile): string | null {
		if (file.extension !== "md") return null;
		const parent = file.parent;
		if (!parent) return null;
		return getTargetCategory(file.path, parent.name, parent.isRoot());
	}

	/** 当前 category 值（来自元数据缓存；仅用于避免无意义写入的预判）。 */
	private currentCategory(file: TFile): unknown {
		return this.app.metadataCache.getFileCache(file)?.frontmatter?.category;
	}

	/**
	 * 唯一的写入入口：事件监听与批量命令共用。
	 * 预判（缓存值）用于跳过无意义写入；回调内二次判断兜底缓存过期的情况。
	 */
	async updateCategory(file: TFile): Promise<UpdateResult> {
		const target = this.targetFor(file);
		if (target === null) return "skipped";

		if (
			!shouldUpdateCategory(
				this.currentCategory(file),
				target,
				this.settings.overwriteExisting,
			)
		) {
			return "skipped";
		}

		try {
			let changed = false;
			await this.app.fileManager.processFrontMatter(file, (frontmatter) => {
				if (
					shouldUpdateCategory(
						frontmatter.category,
						target,
						this.settings.overwriteExisting,
					)
				) {
					frontmatter.category = target;
					changed = true;
				}
			});
			return changed ? "updated" : "skipped";
		} catch (error) {
			console.error(`[category-autofill] 处理 ${file.path} 失败：`, error);
			return "failed";
		}
	}

	/** 防抖调度：同一路径的重复事件合并为一次处理。 */
	private schedule(path: string, action: () => void) {
		const existing = this.pending.get(path);
		if (existing !== undefined) {
			window.clearTimeout(existing);
		}
		const timer = window.setTimeout(() => {
			this.pending.delete(path);
			action();
		}, DEBOUNCE_MS);
		this.pending.set(path, timer);
	}

	private handleCreate(file: TAbstractFile) {
		if (!(file instanceof TFile)) return;
		this.schedule(file.path, () => {
			void this.updateCategory(file);
		});
	}

	private handleRename(file: TAbstractFile, _oldPath: string) {
		if (file instanceof TFile) {
			this.schedule(file.path, () => {
				void this.updateCategory(file);
			});
			return;
		}
		if (file instanceof TFolder) {
			// 文件夹重命名：其下所有文件的父文件夹名已变，逐个更新。
			this.schedule(`dir:${file.path}`, () => {
				for (const markdownFile of this.filesUnder(file.path)) {
					void this.updateCategory(markdownFile);
				}
			});
		}
	}

	private filesUnder(folderPath: string): TFile[] {
		return this.app.vault
			.getMarkdownFiles()
			.filter((f) => f.path.startsWith(`${folderPath}/`));
	}

	/** 全库批量命令（Task 4 中挂到 addCommand）。 */
	async runBatch() {
		const files = this.app.vault.getMarkdownFiles();
		let updated = 0;
		let skipped = 0;
		let failed = 0;

		for (const file of files) {
			const result = await this.updateCategory(file);
			if (result === "updated") updated += 1;
			else if (result === "skipped") skipped += 1;
			else failed += 1;
		}

		new Notice(
			`category 填充完成：更新 ${updated} 个 / 跳过 ${skipped} 个 / 失败 ${failed} 个`,
		);
	}
}

class CategoryAutofillSettingTab extends PluginSettingTab {
	private plugin: CategoryAutofillPlugin;

	constructor(app: App, plugin: CategoryAutofillPlugin) {
		super(app, plugin);
		this.plugin = plugin;
	}

	display() {
		const { containerEl } = this;
		containerEl.empty();

		new Setting(containerEl)
			.setName("覆盖已有的 category 值")
			.setDesc(
				"开启时总是把 category 同步为文件夹名；关闭时只在 category 为空时填充。",
			)
			.addToggle((toggle) =>
				toggle
					.setValue(this.plugin.settings.overwriteExisting)
					.onChange(async (value) => {
						this.plugin.settings.overwriteExisting = value;
						await this.plugin.saveSettings();
					}),
			);
	}
}
```

- [ ] **Step 2: 验证类型检查与构建**

Run: `pnpm typecheck` → Expected: 无输出，退出 0。
Run: `pnpm build` → Expected: typecheck 通过后 esbuild 产出 `main.js`（约 10kb），退出 0。

- [ ] **Step 3: 代码审查要点（自查）**

逐项确认：
1. 事件均已用 `registerEvent` 注册（插件禁用时自动清理）。
2. `onunload` 清理防抖队列，无泄漏定时器。
3. 写入只在 `updateCategory` 一处发生，事件与批量命令共用。
4. `targetFor` 覆盖全部跳过规则（非 md、根目录、隐藏目录）。
5. 回调内二次判断 `shouldUpdateCategory`，兜底缓存过期。
6. `runBatch` 顺序 await，无并发写冲突。

- [ ] **Step 4: 提交**

```bash
git add main.ts
git commit -m "feat(core): 设置页、vault 事件监听与 processFrontMatter 写入"
```

---

### Task 4: 全库批量命令

**Files:**
- Modify: `main.ts`（onload 中注册命令）

**Interfaces:**
- Consumes: `runBatch(): Promise<void>`（Task 3）

- [ ] **Step 1: 在 onload 注册命令**

在 `main.ts` 的 `onload()` 中，`this.addSettingTab(...)` 之后插入：

```ts
		this.addCommand({
			id: "fill-all-categories",
			name: "自动填充 category（全库）",
			callback: () => {
				void this.runBatch();
			},
		});
```

- [ ] **Step 2: 验证类型检查与构建**

Run: `pnpm typecheck` → Expected: 无输出，退出 0。
Run: `pnpm build` → Expected: 退出 0。

- [ ] **Step 3: 提交**

```bash
git add main.ts
git commit -m "feat(command): 全库批量填充命令与结果计数提示"
```

---

### Task 5: README 与最终验证

**Files:**
- Create: `README.md`

**Interfaces:**
- Consumes: 全部已实现功能

- [ ] **Step 1: 写 README.md**

````markdown
# Category Autofill

自动把笔记 frontmatter 中的 `category` 属性填充为该文件**直接父文件夹**的名称，让分类与目录结构保持一致。

## 功能

- **自动监听**：新建 md 文件、移动/重命名文件、重命名文件夹时，实时更新其下文件的 `category`。
- **批量命令**：命令面板执行「自动填充 category（全库）」，一键修复所有历史文件，结束后提示「更新 X 个 / 跳过 Y 个 / 失败 Z 个」。命令幂等，可重复执行。
- **设置项**：「覆盖已有的 category 值」，默认开启。关闭时只在 `category` 为空时填充。

## 行为规则（自动监听与批量命令一致）

- 只处理 Markdown 文件。
- 库根目录的文件不处理。
- 隐藏目录（路径任一段以 `.` 开头，如 `.obsidian`、`.trash`）内的文件不处理。
- 多层子文件夹只取**直接父文件夹名**：`技术分享/前端/x.md` → `category: 前端`。
- `category` 已等于文件夹名时不写入，文件内容零改动。

## 安装

1. 构建：`pnpm install && pnpm build`，产物为 `main.js`。
2. 在库目录 `.obsidian/plugins/` 下新建文件夹 `category-autofill`，拷入 `main.js` 和 `manifest.json`。
3. 重启 Obsidian，在「设置 → 第三方插件」中启用「Category Autofill」。

## 验收清单

1. 在子文件夹新建 md 文件 → category 自动填为文件夹名。
2. 把文件拖到另一个文件夹 → category 变为新文件夹名。
3. 重命名文件夹 → 其下所有文件的 category 变为新文件夹名。
4. 执行全库命令 → 历史文件被批量修复，Notice 计数正确。
5. 关闭「覆盖已有值」后手动填一个 category → 再移动文件，手动值不被覆盖。
6. 库根目录文件、隐藏文件夹（`.obsidian` 等）内文件 → 均不被改动。
7. category 已等于文件夹名 → 文件内容无任何改动。
8. 非 md 文件（图片、PDF）→ 不被改动。

## 已知边界

- 文件在编辑器中有**未保存的正文改动**时，若被写入（新建/移动/批量命令命中），编辑器会重载并丢弃未保存改动。这是 Obsidian 官方 `processFrontMatter` API 的通用行为，非本插件独有；建议操作前先保存正在编辑的文件。
- 无 frontmatter 的文件会自动创建 frontmatter（官方 API 行为）。

## 开发

```bash
pnpm install      # 安装依赖
pnpm dev          # 监听模式构建
pnpm build        # 类型检查 + 生产构建
pnpm typecheck    # 仅类型检查
pnpm test         # node:test 单元测试（纯逻辑模块）
```
````

- [ ] **Step 2: 最终验证（三项全跑）**

Run: `pnpm test` → Expected: PASS，15 个用例全绿。
Run: `pnpm typecheck` → Expected: 无输出，退出 0。
Run: `pnpm build` → Expected: typecheck 通过 + esbuild 产出 main.js，退出 0。

- [ ] **Step 3: 检查 git 状态并提交**

Run: `git status` → Expected: 仅 README.md、main.js（gitignore 已忽略 main.js，不应出现在状态里）。
```bash
git add README.md
git commit -m "docs(readme): 安装说明、行为规则与验收清单"
```

- [ ] **Step 4: 交付检查（自查）**

1. `git log --oneline` 共 7 个提交（设计文档 2 个 + 脚手架 + logic + core + command + readme）。
2. 博客仓库 `.gitignore` 已含 `obsidian-category-autofill`（工作区未提交，属预期）。
3. 向用户交付：插件位置、安装步骤、验收清单、已知边界（如实说明：动态验证需用户在真实 Obsidian 中执行验收清单）。
````

---

## Self-Review 记录

- **Spec 覆盖**：设计文档 §5 路径判定→Task 2；§5.2 写入规则→Task 3 `updateCategory`；§6 事件与防抖→Task 3；§7 手动命令→Task 4；§8 设置页→Task 3；§9 错误处理（try/catch、无 frontmatter）→Task 3 + README；§10 验证→Task 5。验收清单 8 条全部落入 README。
- **占位符**：无 TBD/TODO，所有步骤含完整代码。
- **类型一致性**：`getTargetCategory(path, parentName, parentIsRoot)`、`shouldUpdateCategory(current, target, overwrite)`、`UpdateResult` 在 Task 2/3/4 中签名一致；`runBatch` 由 Task 3 产出、Task 4 消费。
